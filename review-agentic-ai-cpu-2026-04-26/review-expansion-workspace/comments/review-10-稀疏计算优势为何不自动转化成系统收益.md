# 评审意见：10-稀疏计算优势为何不自动转化成系统收益

## 总体评价

主线三的开篇章节，核心判断（MoE 省下计算但引入更难预测的状态运动）精准。`route → locate → move → synchronize` 四步分解是本文的方法论亮点，把抽象的"稀疏计算"翻译成了具体的系统动作链。`cold expert` / `expert skew` / `synchronization chain` 三类代价的分析有深度。

**但存在严重的编辑事故：文件中有两个版本的内容并存。**

---

## 具体修改建议

### 【高优先级 —— 必须立即修复】

**1. 合并或删除重复内容**
- 当前文件结构混乱：
  - 第 1-15 行：子章节标题和提纲要点
  - 第 16-76 行：**第一版正文**（无判断表，无正式章节编号，直接展开 route/locate/move/synchronize 和 cold expert/skew/synchronization 的分析）
  - 第 78-84 行：判断-证据对齐表
  - 第 86-88 行：第 1 节"本章核心判断"（与第 16-25 行内容高度重复）
  - 第 90-139 行：**第二版正文**（有正式章节编号 2-6，内容与前 76 行大量重复）
- **建议**：
  - 方案 A（推荐）：以第 78-139 行的正式版本为骨架，将第 16-76 行中独有的精华内容（如更详细的 cold expert 代价清单、expert skew 的 hotspot 分析）整合进正式版本，然后删除第 16-76 行。
  - 方案 B：以第 16-76 行为主，补充判断表和章节编号，删除 78-139 行。但此版本缺少引用标记，不如正式版本规范。

**2. 图片语法不一致**
- 同前几章，使用 HTML `<img>` 标签。
- **建议**：统一为 Markdown 图片语法。

### 【中优先级】

**3. S 编号未标注全称**
- 判断表中使用 `S008 S035 S036 S037`，正文中提到 `FluxMoE` `FineMoE` `SpecMoEOff` `Wide Expert Parallelism`，但没有明确与 S 编号对应。
- **建议**：`S008`（FluxMoE）、`S035`（Wide Expert Parallelism）、`S036`（FineMoE）、`S037`（SpecMoEOff）。

**4. 第 2 版正文与 11 子章节的边界**
- 第 3 节"cold expert 为什么会吞掉理论收益"和第 4 节"expert skew"在 10 中已经展开，而 11 子章节（专家驻留预取与动态平衡）的核心内容也是 residency / prefetch / balance。
- **建议**：在 10 中压缩对 prefetch 技术细节的展开，仅保留"为什么需要 prefetch"的论证角度（即证明 cold expert 是问题）；把 FluxMoE / FineMoE / SpecMoEOff 的具体机制留给 11。

**5. S035 的引用位置**
- 判断表中 `S035`（Wide Expert Parallelism）在正文中只出现一次（第 4 节），且论证角度偏宏观。
- **建议**：如果 10 的定位是"问题定义"而非"解决方案"，S035 的工业信号可以放在 13（工业界当前吸收到了哪一步）中展开，10 集中用 S008/S036/S037 的学术论文论证问题本身。

### 【低优先级】

**6. 与主线二的衔接**
- 10 作为主线三开篇，没有回顾主线二的结论。
- **建议**：增加一句过渡，如"主线二说明了 KV 生命周期治理把 CPU 推向状态控制面；主线三将说明 MoE 的 expert 驻留与路由平衡问题如何把 CPU 进一步推向动态编排角色"。

---

## 可接受当前状态的点

- `route → locate → move → synchronize` 四步分解是方法论亮点，保留。
- "GPU 少算了不等于系统更轻了"的反直觉切入有效。
- `3.0x`（FluxMoE）和 `2.5x`（SpecMoEOff）的数字支撑具体。
