# 评审意见：08-Prefix Cache 之后的技术演化

## 总体评价

四类演化（routing → retention → events → identity）的梳理是主线二的技术精华。从"本地命中"到"分布式控制面"的升级路径清晰，材料丰富（8 个 S 编号），且能落地到具体工程问题（dirty cache、pinned prefix、multimodal bug）。

**但存在结构和编辑问题：章节编号顺序颠倒，且参考文献后有大量"孤儿内容"。**

---

## 具体修改建议

### 【高优先级 —— 必须立即修复】

**1. 章节编号顺序颠倒**
- 当前第 1 节"本章核心判断"在第 5-7 行，第 0 节"判断-证据对齐表"在第 9-16 行。这与 01-07 的惯例（0 在前，1 在后）相反。
- **建议**：将第 0 节移到第 1 节之前，或统一将所有子章节的判断表编号为第 1 节（如果这是新的风格，则需全局统一）。

处理结论：接受。
说明：`08` 当前确实还是 `1` 在前、`0` 在后。
修改记录：计划把判断表前移，统一回 `0 -> 1 -> 正文展开` 的格式。

**2. 删除或整合第 86-130 行的"孤儿内容"**
- 第 68-84 行是参考文献，结构完整。但第 86-130 行突然出现大量无标题段落，讨论 agentic workload 的 identity 问题和 CPU 角色升级。
- **建议**：
  - 方案 A：将其中关于 identity 的论述整合进第 5 节"第四步演化"中（该节目前较短，可吸收这部分内容）。
  - 方案 B：将第 7 节"为什么这些演化都会落到 CPU 身上"提升为正式章节，并将孤儿内容整合进去。
  - 方案 C：直接删除。

处理结论：接受。
说明：评论成立。参考文献后的孤儿内容质量不差，不应直接丢弃。
修改记录：计划采用接近 A+B 的做法，把 identity 相关内容并回正式 `第 5 节`，并把“为什么这些演化都会落到 CPU 身上”恢复成正式章节。

**3. 图片语法不一致** ✅ 已修复：已统一为 Markdown `![alt](path)` 语法
- 同前几章，使用 HTML `<img>` 标签。
- **建议**：统一为 Markdown 图片语法。

处理结论：不接受新增修改。
说明：这项已修复。
修改记录：维持当前 Markdown 图片语法。

### 【中优先级】

**4. S 编号数量过多且未标注全称**
- 判断表中列出 8 个 S 编号（S011 S016 S041 S042 S044 S045 S046 S047），是 01-10 中最多的，但全部没有全称。
- **建议**：
  - 在判断表中改为带注释格式，如 `S011 (BentoML prefix-aware routing)`。
  - 在正文首次出现时标注全称。
  - 考虑将 8 个材料按功能分组（routing / retention / events / identity），减少读者的认知负担。

处理结论：不接受新增修改。
说明：前半条问题已经处理，现稿已补简称；但“按功能分组降低认知负担”这个方向有价值，不过更适合在正文组织里实现，而不是再改 `S` 编号格式。
修改记录：维持当前简称写法；后续若调整正文，会按 routing / retention / events / identity 的顺序进一步收紧材料出场顺序。

**5. 第 5 节"multimodal / branch-aware identity"偏短**
- 第 5 节只有两段，而前四节都有更充分的展开。identity 是主线二中面向未来的关键方向。
- **建议**：补充更多关于"状态身份如何定义"的技术讨论。例如：
  - 当前 vLLM 的 prefix cache identity 基于什么（block hash + token ids + cache_salt？）
  - 多模态场景下为什么文本前缀不足以代表完整状态
  - 分支感知复用（branch-aware reuse）与 exact prefix 的技术差异

处理结论：接受。
说明：这条评论准确，`identity` 现在是 `08` 最薄的一段。
修改记录：计划把参考文献后的 identity 讨论并回这一节，并补一小段 exact prefix 与 branch-aware / multimodal identity 的差异说明。

**6. 图 2 的复用问题**
- 图 2 使用的是 `agentic-kv-memory-hierarchy.svg`，与 05 图 2 相同。
- **建议**：在图注中明确本节使用此图的具体论证角度（本节强调"事件化需要在完整生命周期中才有意义"，与 05 强调的"跨层级流动"不同）。

处理结论：接受。
说明：复用可接受，但本章视角需要讲清。
修改记录：计划重写图 2 图注，明确它在 `08` 中支持的是“事件化与 retention 必须依附完整 lifecycle”的判断。

### 【低优先级】

**7. 与 07 的衔接**
- 08 开头没有回顾 07 的结论，直接进入 APC 之后的技术演化。
- **建议**：增加一句过渡，如"07 已将 APC 定位为第一代状态复用控制平面；本节讨论它之后出现的四类关键演化"。

处理结论：接受。
说明：这条建议成立。
修改记录：计划在 `08` 开头补一句，从 APC 的边界自然引到后续四类演化。

---

## 可接受当前状态的点

- 四类演化（routing / retention / events / identity）的分类框架成熟。
- 从工程 bug 报告（S044-S047）提炼技术演化方向的手法有洞察力。
- `match_rate_threshold = 0.1` 和 `50ms -> 500ms+` 的具体数字增强了说服力。
