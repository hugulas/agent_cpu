# 评审意见：07-Prefix Cache 是第一代状态复用技术

## 总体评价

核心判断（APC 是"第一代状态复用控制平面"而非局部优化）定位精准。从"解决了什么"到"没有解决什么"的辩证结构是本章的亮点。`5x` TTFT 改善和 `64 -> 8` tokens 的 block size 优化数字具体有力。

**但存在严重编辑问题：第 71-93 行是参考文献之后的"孤儿内容"，没有章节标题，风格与正文不同，疑似未整合的扩展笔记。**

---

## 具体修改建议

### 【高优先级 —— 必须立即修复】

**1. 整合或删除第 71-93 行的"孤儿内容"**
- 第 64-70 行是小结和参考文献，结构完整。但第 71 行开始突然出现一段无标题论述，讨论"worker 之间负载失衡""多模态 identity"等问题，并以大段总结收尾。
- **建议**：
  - 方案 A：如果这段内容有价值，将其整合进第 4 节"APC 没有解决什么"或第 5 节"为什么说它是'第一代'"中，作为补充论证。
  - 方案 B：如果内容重复或质量不足，直接删除。
- 当前放在参考文献之后的形态完全不可接受。

处理结论：接受。
说明：评论成立。参考文献后的无标题补充段现在还在。
修改记录：计划把其中与 APC 边界直接相关的内容并回第 4 节或第 5 节，其余重复部分删除，不再让正文出现在参考文献之后。

**2. 图片语法不一致** ✅ 已修复：已统一为 Markdown `![alt](path)` 语法
- 同 05/06，使用 HTML `<img>` 标签。
- **建议**：统一为 Markdown 图片语法。

处理结论：不接受新增修改。
说明：这项已修复。
修改记录：维持当前 Markdown 图片语法。

### 【中优先级】

**3. 图 1 的论证功能需要澄清**
- 图 1 使用的是 `nvidia-dynamo-agentic-kv-readwrite-2026.webp`（与 05 图 1 相同），但图注说"不是 APC 的实现图，而是用来解释为什么 prefix reuse 会迅速变成控制面问题"。
- **建议**：如果这张图不是 APC 特有的，而是解释 agentic workload 下复用压力的通用图，建议更换为更贴近 APC 本身的图（如 vLLM prefix cache manager 结构图或 TensorRT-LLM early reuse 流程图），否则容易让读者困惑"为什么这里放了一张 Dynamo 的图"。

处理结论：部分接受。
说明：评论方向对，但我暂不准备强行更换图片，因为当前工作区没有更强、可直接复用且证据链完整的 APC 专属图。更现实的做法是把图注写得更明确。
修改记录：计划保留现图，但强化图注与正文说明，明确它在本章只承担“为什么 prefix reuse 很快升级成控制面问题”的背景支撑，而不是 APC 机制图。

**4. S 编号未标注全称** ✅ 已修复：判断表与正文中均已标注全称
- `S010` `S043` 未标注全称。
- **建议**：`S010`（vLLM Automatic Prefix Caching）、`S043`（TensorRT-LLM KV Cache Early Reuse）。

处理结论：不接受新增修改。
说明：正文里已经补过简称。
修改记录：维持当前写法。

**5. 第 4 节与 08 子章节的边界**
- 第 4 节列举了 APC 的四项边界（exact prefix 假设、单机可见性、命中稳定性、多模态 identity），这些恰恰是 08 子章节要展开的内容。
- **建议**：在 07 第 4 节末尾增加一句明确的过渡，如"这些边界催生了下一章讨论的 routing、retention、events 和 identity 演化"，使两章衔接更平滑。

处理结论：接受。
说明：这条建议成立，且改动成本低。
修改记录：计划在 `07` 第 4 节或小结末尾补一句，明确把 APC 的四类边界引到 `08` 的四类演化。

### 【低优先级】

**6. 参考文献日期格式**
- [2] 日期为 `2024-11-08`，[3] 为 `2026-04-17`，[1] 为 `current`。
- **建议**：统一日期格式。

处理结论：接受。
说明：格式仍不统一。
修改记录：计划把 `07` 的参考文献日期口径收紧到 `YYYY-MM` 或 `YYYY-MM-DD`。

---

## 可接受当前状态的点

- "第一代"的历史定位视角成熟。
- 四项边界（exact / 单机 / 稳定性 / 多模态）的列举全面。
- 小结段落的因果链完整。
