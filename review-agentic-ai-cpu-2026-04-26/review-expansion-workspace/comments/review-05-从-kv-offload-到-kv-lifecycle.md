# 评审意见：05-从 KV Offload 到 KV Lifecycle

## 总体评价

主线二的开篇章节，定位准确。从"容量补丁"到"生命周期对象"的概念升级是主线二的核心贡献，论述清晰。`write-once-read-many` 的特征提炼、lifecycle 七阶段分解（创建→保留→迁移→预取→恢复→复用→回收）以及 CPU 四角色升级（state keeper / recovery planner / warm-tier manager / prefetch trigger）都有综述级别的概括力。

---

## 具体修改建议

### 【高优先级】

**1. 图片语法与 01-04 不一致**
- 当前使用 HTML `<img src="..." alt="..." width="760">` 标签，而 01-04 使用标准 Markdown `![alt](path)` 语法。
- **建议**：统一为 Markdown 图片语法，或至少在子章节内部统一。如果必须控制宽度，可考虑在父章节 CSS/样式中统一处理，而不是在 Markdown 中混用 HTML。

**2. S 编号未标注全称** ✅ 已修复：判断表与正文中均已标注全称
- 判断-证据对齐表中使用 `S003 S006 S007 S034`，正文中也出现 `S006` `S007` `S034` 等，但均未标注材料全称。
- **建议**：参照 01-04 的修改方式，在判断表中改为 `S003 (Dynamo agentic); S006 (NOSA); S007 (ScoutAttention); S034 (TensorRT-LLM KV reuse)` 等格式，并在正文首次出现时标注全称。

### 【中优先级】

**3. 图 2 的复用问题**
- 图 2（`agentic-kv-memory-hierarchy.svg`）在 05 中使用，也会在 08、09 中出现（根据文件结构判断）。
- **建议**：如果同一图在多个子章节复用，建议在图注中明确说明其在本节的具体论证功能，避免读者产生"这张图是不是放错了"的困惑。

**4. 第 2 节与 06 子章节的边界**
- 第 2 节已经深入讨论了 NOSA 和 ScoutAttention 的 offload-friendly 设计，而 06 子章节的核心内容也是 NOSA 和 ScoutAttention。
- **建议**：在 05 中压缩对 NOSA/ScoutAttention 技术细节的展开，仅保留"它们如何证明 KV 从容量转向生命周期"的论证角度；把技术细节留给 06。当前 05 第 2 节和 06 存在内容重叠。

### 【低优先级】

**5. 参考文献格式**
- [1] 的日期为 `2026-04-17`，[2] 为 `2025-10-15`，[3] 为 `2026-03-28`，[4] 为 `2025`。日期格式不统一。
- **建议**：统一为 `YYYY-MM-DD` 或 `YYYY-MM` 格式。

---

## 可接受当前状态的点

- 核心判断（KV 从容量对象到生命周期对象）精炼且贯穿全文。
- `11.7x` 读写比作为关键锚点数字，位置醒目，保留。
- lifecycle 七阶段分解有教学价值，保留。
