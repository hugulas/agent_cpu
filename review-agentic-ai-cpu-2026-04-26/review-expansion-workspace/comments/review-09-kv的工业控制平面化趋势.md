# 评审意见：09-KV 的工业控制平面化趋势

## 总体评价

主线二的收尾章节，功能完成度高。三类信号（KV-aware placement / 生命周期显式建模 / 状态变化对外可见）的提炼准确，且能收束到 CPU 四角色升级（state broker / tiering controller / resume-path optimizer / routing advisor）。工业材料（Dynamo / TensorRT-LLM / vLLM / NIXL）的覆盖全面。

**但存在与 08 类似的结构和编辑问题：章节顺序颠倒，参考文献后有巨量"孤儿内容"。**

---

## 具体修改建议

### 【高优先级 —— 必须立即修复】

**1. 章节编号顺序颠倒**
- 同 08，第 1 节在第 5-7 行，第 0 节在第 9-16 行。
- **建议**：统一调整为 0 在前、1 在后的顺序。

处理结论：接受。
说明：`09` 仍存在同样的顺序问题。
修改记录：计划把 `0. 判断表` 前移到 `1. 核心判断` 之前。

**2. 删除或整合第 85-197 行的"孤儿内容"**
- 这是 05-10 中最严重的"孤儿内容"问题：参考文献在第 84 行结束，但第 85-197 行有长达 113 行的无标题段落，涵盖 agentic workload 特征、KV-aware routing、warm tier、event API、cache state visibility 和 CPU 角色升级等内容。
- **建议**：
  - 方案 A（推荐）：将这段内容整合为 09 的正式章节。实际上，这段内容的质量很高，涵盖了第 3-8 节的全部要点。可以将其拆分为：
    - 第 3 节：KV-aware routing（第 91-111 行）
    - 第 4 节：warm tier（第 113-133 行）
    - 第 5 节：event API（第 135-155 行）
    - 第 6 节：cache state visibility（第 157-170 行）
    - 第 7 节：对 AI CPU 的关键意义（第 172-189 行）
    - 第 8 节：小结（第 191-195 行）
  - 这样 09 将从当前简陋的 5 节扩展为结构丰满的 8 节。
  - 方案 B：如果认为当前前 84 行已足够，则直接删除 85-197 行。

处理结论：接受。
说明：这条建议我同意，而且倾向于采纳方案 A。`09` 作为主线二收尾，确实应该更丰满。
修改记录：计划把参考文献后的高质量内容整合为正式章节，尤其保留 `KV-aware routing`、`warm tier`、`event API` 与 `cache state visibility` 这几段。

**3. 图片语法不一致** ✅ 已修复：已统一为 Markdown `![alt](path)` 语法
- 同前几章。
- **建议**：统一为 Markdown 图片语法。

处理结论：不接受新增修改。
说明：这项已修复。
修改记录：维持当前 Markdown 图片语法。

### 【中优先级】

**4. S 编号未标注全称** ✅ 已修复：判断表与正文中均已标注全称
- `S003` `S014` `S034` `S042` 未标注全称。
- **建议**：`S003`（NVIDIA Dynamo agentic inference）、`S014`（NVIDIA Dynamo NIXL）、`S034`（TensorRT-LLM KV reuse）、`S042`（vLLM KV Events Subscriber）。

处理结论：不接受新增修改。
说明：正文里已经补过简称。
修改记录：维持现状。

**5. 第 2 节与 09 整体结构的失衡**
- 当前 09 的正文只有 3 节（2. 为什么工业界会先把 KV 控制平面化；3. 控制平面化具体表现在哪里；4. 为什么这会把 CPU 固定在关键路径上；5. 小结）。内容偏少，与主线二收尾章节的地位不匹配。
- **建议**：如果采纳"方案 A"整合孤儿内容，09 将成为主线二中最充实的章节，与其收尾地位相称。

处理结论：接受。
说明：评论成立，且会随着孤儿内容整合一起解决。
修改记录：计划在整合参考文献后内容时，顺手把 `09` 扩成更完整的收尾章节，而不是保留当前偏薄的前半结构。

**6. 图 2 的论证功能**
- 图 2 使用的是 `nvidia-k8s-disagg-serving-2026.webp`，图注说"说明工业控制平面化不仅是'知道状态在哪'，还包括统一跨层级移动"。
- **建议**：确认此图是否包含 NIXL / transfer API 的具体内容。如果图本身主要展示 PD 分离架构而非 transfer API，建议更换为更直接支撑"unified transfer"判断的图。

处理结论：部分接受。
说明：评论方向对，但当前更现实的做法是强化图注，不急于替换图片。现图更像“跨层级协同与解耦架构”信号，不是 transfer API 细节图。
修改记录：计划把图注改得更准确，避免把它写成 NIXL 机制图；若后续找到更直接的 transfer stack 图，再考虑替换。

### 【低优先级】

**7. 与主线三的衔接**
- 09 作为主线二收尾，应增加一句向主线三（MoE）的过渡。
- **建议**：在小结末尾增加"KV 控制平面化证明了状态对象需要被系统治理；MoE 的 expert 则是另一类需要被治理的状态对象，其治理逻辑将在主线三展开"。

处理结论：接受。
说明：这条建议成立。
修改记录：计划在 `09` 小结末尾补一条明确过渡，把 KV 生命周期治理转向 MoE expert 驻留与编排。

---

## 可接受当前状态的点

- "工业界已把 KV 当成一等系统资源来治理"的判断有力。
- CPU 四角色升级（state broker / tiering controller / resume-path optimizer / routing advisor）是主线二的最佳收束。
- 如果孤儿内容被整合，09 将成为主线二的高光章节。
