# 评审意见：15-从单集群 PD 到 Prefill-as-a-Service

## 总体评价

主线四的技术深化章节。从单集群 PD（解决资源错配）到 PraaS（解决跨池状态编排）的升级路径清晰，`+54%` 吞吐、`-64%` P90 TTFT 和 `+32%` 异构基线三个数字具体且构成完整证据链。第 6 节"边界"的审慎判断（"PraaS 并非普适更优"）体现了综述的严谨性。

**但存在结构问题：章节编号顺序颠倒，参考文献后有大量"孤儿内容"。**

---

## 具体修改建议

### 【高优先级 —— 必须立即修复】

**1. 章节编号顺序颠倒**
- 第 1 节在第 5-7 行，第 0 节在第 9-16 行。
- **建议**：调整为 0 在前、1 在后的顺序。

处理结论：接受。
说明：`15` 当前仍是 `1` 在前、`0` 在后。
修改记录：计划把判断表前移，统一章节顺序。

**2. 删除或整合第 86-150 行的"孤儿内容"**
- 第 75-85 行是参考文献，第 86-150 行有约 65 行无标题段落，涵盖：
  - 第 86-92 行：state-aware service decomposition
  - 第 94-117 行："为什么这一步会显著抬高 CPU 的控制面价值"（四点论述）
  - 第 119-129 行："为什么这还不是默认答案"（边界条件）
  - 第 131-142 行："与 reduced-KV / hybrid-attention 的关系"
  - 第 144-150 行：小结
- 这段内容质量很高，尤其是第 94-117 行对 CPU 控制面价值的展开和第 131-142 行与模型结构演化的关联，是正文前 75 行缺少的。
- **建议**：
  - 方案 A（强烈推荐）：将孤儿内容整合为正式章节：
    - 将第 94-117 行整合为新的"第 5 节：CPU 职责从本地阶段调度器升级成分布式服务编排器"
    - 将第 119-129 行整合为新的"第 6 节：边界——为什么还不是默认答案"（替换当前过于简短的第 6 节）
    - 将第 131-142 行整合为新的"第 7 节：与模型结构演化的关系"
    - 删除当前第 86-93 行的冗余论述
  - 这样 15 的结构将更加丰满。

处理结论：接受。
说明：这条建议我同意。参考文献后的内容质量较高，尤其对 CPU 控制面价值和边界条件的展开值得保留。
修改记录：计划把参考文献后的高质量内容整合回正式章节，并删除其中重复导语。

**3. 图片语法不一致** ✅ 已修复：已统一为 Markdown `![alt](path)` 语法
- 同其他子章节。
- **建议**：统一为 Markdown 图片语法。

处理结论：不接受新增修改。
说明：这项已修复。
修改记录：维持当前 Markdown 图片语法。

### 【中优先级】

**4. S 编号未标注全称** ✅ 已修复：判断表与正文中均已标注全称
- `S001` `S002` `S003` `S015` `S023` 未标注全称。
- **建议**：`S001`（Prefill-as-a-Service）、`S002`（K8s disaggregated LLM）、`S003`（NVIDIA Dynamo）、`S015`（LMCache disaggregated prefill）、`S023`（Prefill-Decode Disaggregation 审慎材料）。

处理结论：不接受新增修改。
说明：这项已修复。
修改记录：维持当前简称写法。

**5. 图 2 的复用问题**
- 图 2 使用的是 `nvidia-k8s-disagg-serving-2026.webp`，与 09 图 2 相同。
- **建议**：复用无问题，但图注需区分视角（15 强调"跨池协调层"，09 强调"统一 transfer API"）。

处理结论：接受。
说明：这条建议成立。
修改记录：计划把图 2 图注改写成“跨池协调层/服务分解层”的视角，不再和 `09` 的工业控制平面视角混在一起。

**6. 第 4 节"PraaS 为什么是实质性升级"偏短**
- 该节只有两段（第 40-47 行），虽然有三项要点，但每项都只是一句话。
- **建议**：为每项要点补充一句解释。例如："prefill 节点可以被当作共享服务"→"这意味着多个 decode 池可以共享同一组 prefill 节点，提高前缀复用率和资源利用率"。

处理结论：接受。
说明：这条建议成立，不过会与整合参考文献后内容一起解决。
修改记录：计划在把孤儿内容并回正式章节时，顺手把第 4 节扩成更完整的三点解释。

### 【低优先级】

**7. 与 16 子章节的衔接**
- 15 结尾没有向 16（这对 CPU 的直接要求）的明确过渡。
- **建议**：在小结末尾增加"PraaS 把 CPU 推向分布式推理控制平面；下一节将具体分析这对 AI CPU 设计目标的直接要求"。

处理结论：接受。
说明：这条建议成立。
修改记录：计划在 `15` 小结末尾补一句，显式指向 `16`。

---

## 可接受当前状态的点

- 三个数字（`+54%` / `-64%` / `+32%`）构成完整的定量证据链。
- "PraaS 的关键不是多一个池，而是多一层控制平面"的判断精准。
- 第 6 节边界条件的审慎处理得当。
