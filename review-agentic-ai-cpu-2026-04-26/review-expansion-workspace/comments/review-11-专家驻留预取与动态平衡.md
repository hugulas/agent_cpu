# 评审意见：11-专家驻留预取与动态平衡

## 总体评价

主线三的技术核心章节，三条路线（FluxMoE / FineMoE / SpecMoEOff）的并列分析成熟。`route → place → move → overlap → rebalance` 的链条提炼准确，且能收束到 CPU 职责升级（维护驻留视图、热点预测、同步窗口）。结构相对规范（0→1→2→3→4→5→6 顺序正确）。

**但存在内容重复问题：第 1-15 行的"提纲要点"与第 45-94 行的正式正文大量重叠。**

---

## 具体修改建议

### 【高优先级】

**1. 删除或整合第 1-15 行的"提纲要点"冗余内容**
- 第 1-15 行包含子章节标题、"本子章节聚焦"、"当前提纲要点"和一段扩展论述（第 16-25 行）。这段扩展论述与第 45-94 行的正式正文几乎完全重复：
  - 第 16-25 行："FluxMoE、FineMoE 和 SpecMoEOff 可以被看作这条链上的三种典型回答"
  - 第 45-94 行：同样论述 + 更详细的展开
- **建议**：删除第 1-25 行（保留标题和父章节信息即可），或仅保留第 1-4 行（标题和父章节），从第 26 行开始作为正式正文。当前状态会让读者在前 25 行读一遍，到第 45 行后又读一遍几乎相同的内容。

处理结论：接受。
说明：评论成立。`11` 开头确实还有一段提纲式冗余。
修改记录：计划删除重复提纲，只保留标题与正式正文入口，不再让读者在前 20 多行读到一套缩略版正文。

**2. 图片语法不一致** ✅ 已修复：已统一为 Markdown `![alt](path)` 语法
- 同 05-10，使用 HTML `<img>` 标签。
- **建议**：统一为 Markdown 图片语法。

处理结论：不接受新增修改。
说明：这项已修复。
修改记录：维持当前 Markdown 图片语法。

### 【中优先级】

**3. S 编号未标注全称** ✅ 已修复：判断表与正文中均已标注全称
- `S008` `S036` `S037` 未标注全称。
- **建议**：`S008`（FluxMoE）、`S036`（FineMoE）、`S037`（SpecMoEOff）。

处理结论：不接受新增修改。
说明：这项已修复。
修改记录：维持当前简称写法。

**4. FineMoE 的论证偏弱**
-  FluxMoE 有 `3.0x` 数字，SpecMoEOff 有 `2.5x` 数字，但 FineMoE（第 3 节）完全没有数字，只有定性描述（"轨迹性和相似性""访问图谱"）。
- **建议**：补充 FineMoE 论文中的关键数字（如预取命中率、miss 降低百分比或吞吐提升），否则该节在"量化支撑"标准上明显弱于其他两节。

处理结论：接受。
说明：这条建议成立，且是 `11` 当前最明显的支撑短板。
修改记录：计划补充 FineMoE 的关键量化结果，并把其意义明确挂回 `prefetch / residency / balance` 这条链条。

**5. 与 10 子章节的边界**
- 11 与 10 都讨论了 FluxMoE / FineMoE / SpecMoEOff，10 重在"为什么需要这些方案"，11 重在"三条方案分别是什么"。
- **建议**：在 11 第 1 节核心判断末尾增加一句过渡，明确"上一章定义了问题，本章展开解法"，避免读者感觉两章内容重复。

处理结论：接受。
说明：这条建议成立。
修改记录：计划在 `11` 开头补一句，从 `10` 的问题定义过渡到 `11` 的三条解法路线。

### 【低优先级】

**6. 图 1 与 10 的图 1 重复**
- 11 图 1 使用的是 `fluxmoe-01.png`，与 10 图 1 相同。
- **建议**：复用本身无问题，但图注需明确本节视角（本节强调"驻留控制是一等系统对象"，10 强调"专家是否已经就位"）。

处理结论：接受。
说明：这条建议合理，且能直接降低重复感。
修改记录：计划改写图 1 图注，明确 `10` 和 `11` 的视角区别。

---

## 可接受当前状态的点

- 三条路线并列比较的结构清晰。
- `residency control` 概念的提炼准确。
- 第 5 节"这三条路线共同说明了什么"的总结段落有力。
