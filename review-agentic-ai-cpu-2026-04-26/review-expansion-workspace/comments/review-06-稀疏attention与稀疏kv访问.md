# 评审意见：06-稀疏 Attention 与稀疏 KV 访问

## 总体评价

前半部分（第 1-74 行）是一篇结构完整的子章节，核心判断清晰，NOSA 和 ScoutAttention 的论证角度合理。`policy engine` 的提炼准确。

**但存在严重编辑事故：第 74 行参考文献之后，从第 82 行到第 174 行出现了几乎完整的另一版草稿内容，且没有章节标题。** 这导致文件中有两套并行的 4-8 节（ScoutAttention / sparse access policy / metadata / agentic workload / 小结），严重破坏综述的可读性和专业性。

---

## 具体修改建议

### 【高优先级 —— 必须立即修复】

**1. 删除第 74 行之后的全部冗余内容**
- 第 74 行是参考文献结束，第 82-174 行是未整合的草稿残片。
- 具体包括：重复的"4. ScoutAttention 代表了什么"、重复的"5. 为什么 sparse access 会把 CPU 推向更细粒度 policy"、重复的"6. 稀疏访问为什么会放大 metadata 和 policy 的重要性"、重复的"7. 为什么这对 agentic workload 特别重要"、重复的"8. 小结"。
- **建议**：直接删除第 75-174 行，或将其与第 1-74 行的内容合并后删除冗余。当前状态完全无法作为综述正文使用。

**2. 图片语法不一致** ✅ 已修复：已统一为 Markdown `![alt](path)` 语法
- 同 05，使用 HTML `<img>` 标签而非 Markdown 图片语法。
- **建议**：统一为 `![alt](path)` 格式。

### 【中优先级】

**3. 第 5 节 Kimi Linear 的引用位置**
- 判断-证据对齐表中列出 `S013`，但 Kimi Linear 只在第 5 节出现一次，且论证角度（reduced-KV 改变成本结构）与"稀疏 attention"主题有一定距离。
- **建议**：要么在 06 中增加对 Kimi Linear 与 sparse KV access 关系的更明确说明，要么将 S013 移到 05 的判断表中（作为 KV 总量下降后 placement 价值上升的证据），减少 06 的注意力分散。

**4. S 编号未标注全称** ✅ 已修复：判断表与正文中均已标注全称
- `S006` `S007` `S013` 在判断表和正文中均未标注全称。
- **建议**：参照 01-04 的修改方式补充。

### 【低优先级】

**5. 第 6 节"边界"段落**
- 第 66 节提到 `material/gap-audit.md`，但该文件路径在项目中是否存在需要确认。如果 gap-audit 不是公开可引用的材料，建议改为更泛化的表述，如"公开资料中仍缺少对 sparse KV policy 误判代价的完整实测"。

---

## 可接受当前状态的点

- 前半部分的"policy engine"提炼准确。
- NOSA `2.3x` 和 ScoutAttention `2.1x` 的数字支撑有力。
- 第 6 节的审慎判断（"收益方向已证明，但代价函数不完整"）体现了综述的严谨性。
